services:
  jetbrains-ai-gemini-local-proxy:
    build: .
    container_name: jetbrains-ai-gemini-local-proxy
    ports:
      - "5003:5003"
    env_file:
      - .env
    environment:
      PROXY_PORT: "5003"
      DEFAULT_MODEL: "gemini-2.5-flash"
      LOG_LEVEL: "DEBUG"
    restart: always
